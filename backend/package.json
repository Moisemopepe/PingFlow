{
  "name": "pingflow-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Local backend for PingFlow real network diagnostics.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  }
}
