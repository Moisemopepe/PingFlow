services:
  - type: web
    name: pingflow-api
    runtime: docker
    rootDir: backend
    plan: free
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: PINGFLOW_PING_MODE
        value: tcp
